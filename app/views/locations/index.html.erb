<div class="padded">
	<%= render partial: 'common/application_header' %>
	<div class="container">
		<h2 class="title centered">Venues</h2>
		<p class="centered"><%= link_to "Add New Location", new_location_path %></p>
		<% @locations.each do |location| %>
			<p><%= link_to location.venue, location_path(location) %></p>
		<% end %>
	</div>
</div>